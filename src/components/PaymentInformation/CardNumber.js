import styled from 'styled-components';

import { default as DefaultText } from '../Text';

const CardNumber = styled(DefaultText)`
  grid-column-start: 2;
  grid-row-start: 3;
`;

export default CardNumber;

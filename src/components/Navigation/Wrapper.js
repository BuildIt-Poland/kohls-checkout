import styled from 'styled-components';

import { colorLightGrey, borderWidthThin } from '../../styles/designTokens';

const Wrapper = styled.div`
  width: 100%;
  border-bottom: ${borderWidthThin} solid ${colorLightGrey};
`;

export default Wrapper;

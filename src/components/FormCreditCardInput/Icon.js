import styled from 'styled-components';

import PaymentCard from '../PaymentCard';

const Icon = styled(PaymentCard)`
  position: absolute;
  top: 50%;
  right: 1rem;
  margin-top: -10px;
`;

export default Icon;

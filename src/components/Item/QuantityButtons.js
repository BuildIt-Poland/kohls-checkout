import styled from 'styled-components';

const QuantityButton = styled.button`
  font-size: 2.2rem;
  font-weight: bold;
  background: none;
  box-shadow: none;
  border: 0;
`;

export default QuantityButton;

import styled from 'styled-components';

import { demoGreen } from '../../styles/designTokens';
import ContentBase from '../Content';

const Content = styled(ContentBase)`
  background: ${demoGreen};
`;

export default Content;

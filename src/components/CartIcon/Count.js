import styled from 'styled-components';

import { colorWhite, fontWeightBold, fontSmall } from '../../styles/designTokens';

const Count = styled.div`
  position: absolute;
  color: ${colorWhite};
  font-weight: ${fontWeightBold};
  font-size: ${fontSmall};
  padding-bottom: 0.7rem;
`;

export default Count;
